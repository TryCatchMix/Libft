# Libft
Proyecto en C

Este proyecto consiste en implementar una **biblioteca propia en C** que replica funciones estándar de la biblioteca C (**libc**). Incluye funciones como:

- `malloc`
- `free`
- `strlen`
- `strdup`
- Entre otras.

El objetivo es familiarizarse con el manejo de **memoria** y las funciones básicas del lenguaje C, mejorando el conocimiento sobre la **manipulación de punteros** y la **gestión de recursos**.
